// Ambient declaration for the CodinGame stdin reader, available globally to all
// puzzle solution files (see CLAUDE.md). `print`/`console.log` are typed via @types/node.
declare function readline(): string
