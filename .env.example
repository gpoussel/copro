PG_HOSTNAME=localhost
PG_PORT=5432
PG_USERNAME=postgres
PG_PASSWORD=postgres

# CodinGame authentication: value of the "rememberMe" cookie from a logged-in
# browser session (the only accepted auth method).
CG_REMEMBER_ME=
# Optional: override the user id if it cannot be derived from the cookie.
# CG_USER_ID=
